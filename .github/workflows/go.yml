name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    # - uses: actions/checkout@v3

    # - name: Set up Go
    #   uses: actions/setup-go@v3
    #   with:
    #     go-version: '>=1.20.0'

    # - name: Build
    #   run: go build -v ./...
      -
        name: Build and push
        uses: docker/build-push-action@v4
        with:
          push: true
          context: .
          file: ./Dockerfile
          tags: simon-ding/airdrop-bot:latest
          secrets: |
            GIT_AUTH_TOKEN=${{ secrets.MYTOKEN }}
